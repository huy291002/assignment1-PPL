
                x: integer = 65;
                inc: function void(out n: integer, delta: integer)
                {
                n = n + delta;
                }
                inc(x, delta);
                hello(x);
                
        